{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/auth/:path*", "destination": "https://pitwall-backend.vercel.app/auth/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
